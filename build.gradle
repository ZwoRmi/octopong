repositories {
    mavenCentral()
}
apply plugin: 'idea'
apply plugin: 'java'
compileJava.options.encoding = 'UTF-8'

dependencies {
    testCompile 'junit:junit:4.10'
}

test {
    testLogging {
        events 'failed'
    }
}
